moteur_inference :
- créer tt la librairies correspondant au type BF
- finir moteur_inference

Problème :
- limite aléatoire de caractère pour ajout_proposition() sinon ça plante
- bc.h
typedef struct elemRegle{
    Regle rgl; => Regle* rlg; // pour les byAdressParameter(pour moteur_inference())
    struct elemRegle* suiv;
}ElemRegle;